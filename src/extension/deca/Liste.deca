class Liste {
	float contenu;
	Liste suivant;
	float valeur_k;

	void init(float contenu, Liste suivant, float valeur_k){
		this.contenu = contenu;
		this.suivant = suivant;
		this.valeur_k = valeur_k;
	}
}
{
	Liste ac;
	Liste ab;
	Liste aa;
	Liste curr;
	


	ac.init(0.0,null,0.0);
	ab.init(0.0,ac,0.0);
	aa.init(0.0,ab,0.0);
	
	curr=aa;
	while(curr != null){
		println(curr.contenu);
		curr=curr.suivant;
	}
}
